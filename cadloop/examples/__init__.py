"""cadloop.examples — 可运行的样例（通道的验收用例）。

``flexure_hinge`` 是**golden example**：它同时充当

1. 通道端到端可用性的验收测试（几何 → 网格 → 求解 → 判据 → 报告 → 账本）
2. 各向异性/打印方向分析的演示（同一模型两种摆放）
3. 解析解对照的载体（叶片式柔性铰链有闭式刚度公式）

⚠️ 注意：样例件的存在是为了验证**通道**，不是替代课程设计。真实作品零件
的建模与判据设计仍由少爷亲手完成。
"""
